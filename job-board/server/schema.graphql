type Query {
    job(id: ID!): Job
    jobs: [Job]
}

type Company {
    id: String!
    name: String
    description: String
}

type Job {
    id: ID!
    title: String
    company: Company
    description: String
}